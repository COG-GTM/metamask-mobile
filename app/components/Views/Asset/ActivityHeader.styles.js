import { StyleSheet } from 'react-native';

const styleSheet = () =>
StyleSheet.create({
  wrapper: {
    paddingHorizontal: 16
  },
  title: {
    paddingVertical: 8
  }
});

export default styleSheet;