diff --git a/node_modules/@metamask/react-native-button/Button.js b/node_modules/@metamask/react-native-button/Button.js
index 46dc1e0..93c5052 100644
--- a/node_modules/@metamask/react-native-button/Button.js
+++ b/node_modules/@metamask/react-native-button/Button.js
@@ -12,17 +12,24 @@ import coalesceNonElementChildren from './coalesceNonElementChildren';
 
 const systemButtonOpacity = 0.2;
 
+// RN 0.77 removed Text.propTypes / TouchableOpacity.propTypes / ViewPropTypes
+// (all deprecated since 0.69). Fall back to PropTypes.any so this 3rd-party
+// component keeps working.
+const _textPropTypes = (Text && Text.propTypes) || {};
+const _touchablePropTypes = (TouchableOpacity && TouchableOpacity.propTypes) || {};
+const _viewPropTypes = (ViewPropTypes && typeof ViewPropTypes === 'object') ? ViewPropTypes : {};
+
 export default class Button extends Component {
   static propTypes = {
-    ...TouchableOpacity.propTypes,
+    ..._touchablePropTypes,
     accessibilityLabel: PropTypes.string,
-    allowFontScaling: Text.propTypes.allowFontScaling,
-    containerStyle: ViewPropTypes.style,
-    disabledContainerStyle: ViewPropTypes.style,
+    allowFontScaling: _textPropTypes.allowFontScaling || PropTypes.bool,
+    containerStyle: _viewPropTypes.style || PropTypes.any,
+    disabledContainerStyle: _viewPropTypes.style || PropTypes.any,
     disabled: PropTypes.bool,
-    style: Text.propTypes.style,
-    styleDisabled: Text.propTypes.style,
-    childGroupStyle: ViewPropTypes.style,
+    style: _textPropTypes.style || PropTypes.any,
+    styleDisabled: _textPropTypes.style || PropTypes.any,
+    childGroupStyle: _viewPropTypes.style || PropTypes.any,
   };
 
   render() {
